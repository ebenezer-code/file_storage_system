/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          hostname: "industrious-crocodile-420.convex.cloud",
        },
      ],
    },
  };
  
export default nextConfig;
  